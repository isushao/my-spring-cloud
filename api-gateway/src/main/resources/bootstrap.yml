server:
  port: 8066

spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: api-gateway-server
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        file-extension: yaml